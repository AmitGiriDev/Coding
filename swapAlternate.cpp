#include <bits/stdc++.h>
using namespace std;

void printArray(int arr[], int n)
{
    for (int i = 0; i < n; i++)
    {
        cout << arr[i] << " ";
    }
}

int maxArea(vector<int>& height {
    

   }

int main()
{
    int cont[] = {1, 2, 3, 4, 5, 5};
    int size = sizeof(cont) / 4;
    cout << size;
    printArray(cont, size);
    return 0;
}
